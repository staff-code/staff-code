import {css} from "@sagittal/general"

const COPY_LINK_CSS = css`
.sc-copy-link {
    margin: 2px;
    cursor: pointer;
    font-family: sans-serif;
    font-size: 0.75em;
}
`

export {
    COPY_LINK_CSS,
}
